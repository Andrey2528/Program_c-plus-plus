/*18.	Введіть з клавіатури ціле число від 1 до 100. У разі введення неправильного значення надрукуйте повідомлення про помилку
на екрані і завершіть виконання програми. */
#include <iostream>
using namespace std;
int main()
{
	setlocale(LC_ALL, "Russian");
	float a;
	cout << "Введите число от 1 до 100:\n";
	cin >> a;
	if (!((a >= 1) && (a <= 100)))
		cout << "Ошибка!!! \n" << endl;
	return 0;
}